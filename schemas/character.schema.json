{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CharacterDataset",
  "type": "object",
  "required": ["metadata", "items"],
  "properties": {
    "metadata": {
      "type": "object",
      "required": ["dataset", "version", "generatedAt", "language", "region", "verified", "source"],
      "properties": {
        "dataset": { "type": "string", "const": "characters" },
        "version": { "type": "string" },
        "generatedAt": { "type": "string", "format": "date-time" },
        "language": { "type": "string" },
        "region": { "type": "string" },
        "verified": { "type": "boolean" },
        "source": { "type": "string" }
      }
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id", "slug", "title", "description", "status", "tags",
          "relationships", "verified", "source", "sourceUrl",
          "createdAt", "updatedAt", "verifiedAt", "version",
          "gender", "role"
        ],
        "properties": {
          "id": { "type": "string", "pattern": "^character_[a-z0-9_]+$" },
          "slug": { "type": "string", "pattern": "^[a-z0-9-]+$" },
          "title": { "type": "string" },
          "description": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["confirmed", "official", "community", "coming_soon", "deprecated"]
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" }
          },
          "relationships": {
            "type": "object",
            "properties": {
              "locations": { "type": "array", "items": { "type": "string" } },
              "vehicles": { "type": "array", "items": { "type": "string" } },
              "weapons": { "type": "array", "items": { "type": "string" } },
              "trailers": { "type": "array", "items": { "type": "string" } }
            },
            "additionalProperties": false
          },
          "verified": { "type": "boolean" },
          "source": { "type": "string" },
          "sourceUrl": { "type": "string", "format": "uri" },
          "createdAt": { "type": "string" },
          "updatedAt": { "type": "string" },
          "verifiedAt": { "type": "string" },
          "version": { "type": "string" },
          "gender": { "type": "string", "enum": ["female", "male", "unknown", "multiple"] },
          "role": { "type": "string", "enum": ["protagonist", "supporting", "antagonist", "npc"] },
          "voiceActor": { "type": ["string", "null"] }
        }
      }
    }
  }
}
